services:
  incrementum:
    build:
      context: .
      args:
        PIPENV_INSTALL_ARGS: --system --deploy
    image: incrementum
    command: python main.py
    extra_hosts:
      - host.docker.internal:host-gateway
    volumes:
      - .:/app
